# Repository Access Architecture

## 🎯 Как Claude Agent SDK получает доступ к коду

### Development (локально)
```
REPO_CLONE_PATH=/Users/admin/Documents/Awkward CRM
```

- **Прямой доступ** к рабочей директории проекта
- ✅ Всегда актуальный код (включая uncommitted изменения)
- ✅ Нет задержек на синхронизацию
- ⚠️ Может видеть `.env` и другие локальные файлы

### Production (на сервере)
```
REPO_CLONE_PATH=/opt/repo (внутри Docker контейнера)
```

**Host система:**
- Клон репозитория в `/opt/awkward-crm-repo`
- Read-only permissions (555)
- Папка `ai_agent/` удалена (чтобы агент не анализировал свой код)

**Docker container:**
- Volume mount: `/opt/awkward-crm-repo:/opt/repo:ro`
- `:ro` = read-only mount
- Claude Agent SDK работает ТОЛЬКО с этой копией

## 🔄 Синхронизация кода

### Автоматическое обновление (GitHub Actions)

При каждом деплое через `.github/workflows/deploy-ai-agent.yml`:

```bash
# 1. Проверяем существует ли клон
if [ ! -d "/opt/awkward-crm-repo" ]; then
  # Клонируем если нет
  git clone https://github.com/awkward-media/awkward-crm.git /opt/awkward-crm-repo
else
  # Обновляем если есть
  cd /opt/awkward-crm-repo && git pull origin main
fi

# 2. Удаляем папку ai_agent (чтобы не анализировал себя)
rm -rf /opt/awkward-crm-repo/ai_agent

# 3. Делаем read-only
chmod -R 555 /opt/awkward-crm-repo

# 4. Монтируем в Docker контейнер
docker run -v /opt/awkward-crm-repo:/opt/repo:ro ...
```

**Когда обновляется:**
- ✅ При каждом деплое AI Agent Service (изменения в `ai_agent/`)
- ✅ При каждом деплое Main App (можно добавить в workflow)
- ✅ Можно триггерить вручную через GitHub Actions UI

## 🚫 Что исключено из анализа

### Через `.claudeignore` (автоматически)
```
ai_agent/              # Код самого агента
node_modules/          # Зависимости
.next/                 # Build артефакты
.env                   # Секреты
.git/                  # Git метаданные
```

### Через физическое удаление
```bash
rm -rf /opt/awkward-crm-repo/ai_agent
```

Папка `ai_agent/` полностью удалена из клона перед анализом.

## 🔐 Безопасность

### Почему это безопасно:

1. **Изолированный клон**: Claude Agent SDK НЕ имеет доступа к:
   - Продакшен файлам приложения
   - Рабочим директориям других контейнеров
   - Базе данных (только через API)

2. **Read-only mount**: Даже если бы Claude попытался изменить файл, Docker `:ro` флаг заблокирует запись

3. **Permissions**: `chmod 555` = только чтение для всех пользователей

4. **No secrets**: `.env` файлы исключены через `.claudeignore`

5. **No self-modification**: Папка `ai_agent/` удалена, агент не может изменить свой код

## 📊 Диаграмма архитектуры

```
┌─────────────────────────────────────────────────────────┐
│                    GitHub Repository                     │
│              awkward-media/awkward-crm                   │
└────────────────────┬────────────────────────────────────┘
                     │
                     │ GitHub Actions Deploy
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│                  Production Server                       │
│                                                          │
│  /opt/awkward-crm-repo (git clone)                      │
│  ├── src/                                                │
│  ├── components/                                         │
│  ├── seo_engine/                                         │
│  ├── .claudeignore                                       │
│  └── [ai_agent/ - REMOVED]                              │
│                                                          │
│  Permissions: 555 (read-only)                           │
└────────────────────┬────────────────────────────────────┘
                     │
                     │ Volume mount (:ro)
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│            Docker: awkward-crm-ai-agent                 │
│                                                          │
│  /opt/repo (mounted from /opt/awkward-crm-repo)         │
│                                                          │
│  Claude Agent SDK:                                       │
│  - workingDirectory: /opt/repo                          │
│  - allowedTools: Read, Grep, Glob, Bash                 │
│  - permissionMode: bypassPermissions                    │
│                                                          │
│  ✅ CAN: Read source code                               │
│  ✅ CAN: Search files                                   │
│  ✅ CAN: Run git commands                               │
│  ❌ CANNOT: Modify files (read-only)                    │
│  ❌ CANNOT: Access production files                     │
│  ❌ CANNOT: See own code (ai_agent/ removed)            │
└─────────────────────────────────────────────────────────┘
```

## 🧪 Тестирование доступа

### Локально
```bash
cd ai_agent
npm run test:local "Покажи структуру проекта"
```

Должно показать файлы из `/Users/admin/Documents/Awkward CRM`

### Production
```bash
# SSH на сервер
ssh user@server

# Проверить клон существует
ls -la /opt/awkward-crm-repo

# Проверить permissions
stat /opt/awkward-crm-repo

# Проверить что ai_agent удален
ls /opt/awkward-crm-repo | grep ai_agent  # Не должно быть вывода

# Проверить Docker mount
docker inspect awkward-crm-ai-agent | grep -A5 Mounts
```

## 🔧 Ручное обновление (если нужно)

```bash
# SSH на сервер
ssh user@server

# Обновить клон
cd /opt/awkward-crm-repo
git pull origin main
rm -rf ai_agent
chmod -R 555 .

# Перезапустить контейнер
docker restart awkward-crm-ai-agent
```

## ❓ FAQ

**Q: Как часто обновляется код для анализа?**
A: Автоматически при каждом деплое через GitHub Actions

**Q: Видит ли Claude uncommitted изменения?**
A: Нет, только committed код из GitHub

**Q: Может ли Claude изменить production код?**
A: Нет, работает с отдельным read-only клоном

**Q: Почему удаляем ai_agent/?**
A: Чтобы агент не анализировал свой собственный код, это может привести к confusion

**Q: Что если нужно проанализировать local изменения?**
A: Для этого есть dev режим с `REPO_CLONE_PATH=/Users/admin/Documents/Awkward CRM`
